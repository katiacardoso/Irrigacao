//Salvando o Bancada noo banco
  import 'package:app_flutter/model/user_cadastroBancada.dart';

Future<Bancada> inserirBancada(Bancada bancada)async{
   // Database dbBancada = await db;
    //bancada.id = await dbCliente.insert(bancadaTable, cliente.toMap());
    return bancada;
  }